version: 2.1
jobs:
  build:
    docker:
      - image: kavithareddy/terraform-heroku:nodejs
    steps: 
      - checkout 
      - run:  | 
         pwd
         ls
         cat main.tf
         terraform init 
         terraform plan 
         terraform apply -auto-approve 